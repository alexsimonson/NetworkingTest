using UnityEngine;

public class DeathScreenUI : MonoBehaviour
{
    void Awake()
    {
        // gameObject.SetActive(false);       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
